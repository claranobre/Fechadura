#include <stdio.h>
#include <stdlib.h> //para usar o comando system
//#include <conio.h> // para a função getch

main ()
{
    while(1)
    {

        char string[5];
        char enter[2];
        printf ("\n Digite a senha de 5 digitos: ");
        string[0] = getch();
        printf ("*");
        string[1] = getch();
        printf ("*");
        string[2] = getch();
        printf ("*");
        string[3] = getch();
        printf ("*");
        string[4] = getch();
        printf ("*");
        string[5] = getch();
        printf ("*");

        enter[0] = getch();
        if(enter[0] == 35)
        {
            if((string[0] == 'f') && (string[1] == 'u') && (string[2] == 'l') && (string[3] == 'a') && (string[4] == 'n') && (string[5] == 'o')) // senha correta é "fulano".
            {
                system("cls"); // limpa a tela.
                printf ("\n\n\t\tAcesso permitido!\n\n\t\tparabens!\n\t\to que voce deseja fazer agora?\n\n\n\n\n\n\n");
                getchar(); // quando precionar o "ENTER" continua o programa.
                system("cls"); // limpa a tela.
            }
            else
            {
                system("cls"); // limpa a tela.
                printf ("\n\n\t\tAcesso negado!\n\n\t\tvoce não tem permicao para acesso!\n\n\n\n\n\n\n\n\n\n\n");
                getchar(); // quando precionar o "ENTER" continua o programa.
                system("cls"); // limpa a tela.
            }
        }
    }
}